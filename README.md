# discord-support-agent

AI agent that monitors Discord servers and notifies about support requests.

Uses Apple Foundation Models for local, cost-free inference.

## Development

```bash
uv sync
uv run python main.py
```